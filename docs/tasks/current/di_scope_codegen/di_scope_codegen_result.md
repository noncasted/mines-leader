## Scope codegen — Результат

### Статус: Не начато

### Что сделано

—

### Измененные файлы

| Файл | Что изменено | Шаг | Evidence |
|------|-------------|-----|----------|

### Проверки

| Проверка | Шаг | Результат |
|----------|-----|-----------|
| Граф с рёбрами строится для `LoadGlobal` и `CardFactory.Build` | 0 | — |
| Снятая регистрация → `CINGR003` с именем параметра | 0 | — |
| Цикл → `CINGR004` с полным путём | 0 | — |
| Класс скоупа для `LoadGlobal` компилируется | 1 | — |
| Ноль обращений к `IResolvePlan` в сгенерированном классе | 1 | — |
| Порядок маркерных массивов = порядок регистрации | 1 | — |
| `Transient` не имеет поля, только метод | 1 | — |
| `[ContainerRuntimeScope]` отключает генерацию точечно | 1 | — |
| `Resolve` на сгенерированном скоупе не создаёт объектов | 2 | — |
| Дебагер отличает сгенерированный скоуп от рантайм-ного | 2 | — |
| Плей-мод: меню + матч против бота | 2 | — |
| `CardScope` / `GamePlayerScope` покрыты генерацией | 2 | — |
| Бенчмарк: третья колонка рядом с VContainer и планом | 3 | — |
| Корней под `[ContainerRuntimeScope]`: сколько | 4 | — |

### Бенчмарк

Три стороны на одном графе: VContainer / рантайм-план / сгенерированный класс.
Заполняется шагом 3, пересдаётся после шага 4.

### Не сделано / отложено

—
