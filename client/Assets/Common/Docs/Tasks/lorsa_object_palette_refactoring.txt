+1. ObjectPalette и ArchivePalette удалить. Сделать один класс ObjectPalette
+2. Сделать общую ObjectCard вместо ArchivePaletteEntryView и ObjectPaletteEntryView
3. убрать из палитры всю информацию о текущих картах
        BookObjectType LastTab { get; }
        IViewableProperty<IReadOnlyList<ObjectPaletteEntryView>> CurrentGroup { get; }
        Guid CurrentFolder { get; set; }
+4. ObjectPaletteGroup удалить
+5. Вся логика контроля групп уходит из палитры в ArchiveFolders или ObjectTabs. Палитра должна работать как сейчас работает ObjectPaletteGroup.

6. Перетаскивание карт сделать через общий ObjectCardMover, который сам обрабатывает кейсы с архивом и дропом на уровень
7. в ObjectPalette должен быть метод Add() вместо полного перерисовывания каждый раз
+8. Разные префабы карточек на каждый тип объектов

+9. папку Folders переместить в Archive, все классы переименовать в ArchiveFolder
+10. Вместо FolderPaletteEntryPointerHandler и ObjectPaletteEntryPointerHandler использовать общий UIElementPointerHandler
+11. Все создание объектов вынести в Level/Edit/ObjectCreation
+12. Вынести информацию о папках из IdentityScheme в глобальную ArchiveScheme
+13. ObjectPaletteEntryData все EntryData удаляем, информацию берем из самой ObjectCard

Структура
    Edit/
        /Common
            /ObjectCard
            /ObjectsPalette - Show(IObjectScheme[] objects)
        /Archive
            /Folders
            ObjectsArchive.cs - агрегатор, который слушает текущую папку, сортирует объекты и кладет в палитру
        /Palette
            /Tabs
            LevelObjectsPalette.cs
        /ObjectCreation    
        /Services
            ObjectCardMover.cs - двигает карточку и отдает на обработку LevelObjectsPalette.cs или LevelObjectDrop.cs
                               - должен слушать списки карточек во всех палитрах, чтобы начать тащить
            LevelObjectDrop.cs
    Schemes/
        - ArchiveScheme.cs - информация о папках и их содержимом
        - FolderScheme.cs - информация о папке, ее типе и тд
           